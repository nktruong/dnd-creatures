<script>
  // -- Randomizer route --
  // 1. user presses randomizer button
  // 2. the program chooses a random # based on the monster count provided by API
  // 3. then again through the API, chooses a random result (monster)
  // 4. with the monster result, accesses the info about that creature
  // 5. display monster and info on the card
</script>
